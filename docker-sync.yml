version: "2"

options:
  max_attempt: 5

syncs:
  docker-sync:
    sync_strategy: 'native_osx'
    src: './'
    sync_excludes: ['./web/sites/default/files/php/twig/', '.gitignore', '.git/', '.idea/']
