langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_landing_feature_layout
    - node.type.landing
id: node.landing.field_landing_feature_layout
field_name: field_landing_feature_layout
entity_type: node
bundle: landing
label: 'Secondary header'
description: 'Secondary header uses a 400px heigh image, the content has a white background overlapping the bottom of the hero image and you don't need to upload a mobile image for it. It will crop the right image for mobile usage.'
required: false
translatable: false
default_value:
  -
    value: 0
default_value_callback: ''
settings:
  on_label: 'On'
  off_label: 'Off'
field_type: boolean
