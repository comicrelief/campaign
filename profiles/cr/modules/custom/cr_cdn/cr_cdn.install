<?php

/**
 * @file
 * Custom module: cr_cdn is used to enable custom cdn config.
 */

/**
 * Uses hook_install().
 */
function cr_cdn_install() {
  Drupal::configFactory()->getEditable('cdn.settings')->set('status', 1)->save(TRUE);
}
