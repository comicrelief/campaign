{#
/**
 * @file
 * Template for a 5 regions panel layout.
 *
 * This template provides 5 regions layout. First region full width and other regions 1200px max-width.
 *
 * Variables:
 * - $id: An optional CSS id to use for the layout.
 * - $content: An array of content, each item in the array is keyed to one
 *   panel of the layout. This layout supports the following sections:
 *   - $content.first Content in the first region.
 *   - $content.second Content in the second region.
 *   - $content.third Content in the third region.
 *   - $content.fourth Content in the fourth region.
 *   - $content.fifth Content in the fifth region.
 */
#}
<div class="cr-layout-fivefull" {% if css_id %}{{ css_id }}{% endif %}>
  {% if content.first|length > 2 %}
  <div class="first-region">
    {{ content.first }}
  </div>
  {% endif %}
  {% if content.second|length > 2 %}
  <div class="second-region">
    {{ content.second }}
  </div>
  {% endif %}
  {% if content.third|length > 2 %}
  <div class="third-region">
    {{ content.third }}
  </div>
  {% endif %}
  {% if content.fourth|length > 2 %}
  <div class="fourth-region">
    {{ content.fourth }}
  </div>
  {% endif %}
  {% if content.fifth|length > 2 %}
  <div class="fifth-region">
    {{ content.fifth }}
  </div>
  {% endif %}
</div>
