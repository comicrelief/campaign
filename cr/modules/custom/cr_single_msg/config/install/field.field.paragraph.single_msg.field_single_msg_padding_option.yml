langcode: en
status: true
dependencies:
  config:
    - field.storage.paragraph.field_single_msg_padding_option
    - paragraphs.paragraphs_type.single_msg
  module:
    - options
id: paragraph.single_msg.field_single_msg_padding_option
field_name: field_single_msg_padding_option
entity_type: paragraph
bundle: single_msg
label: 'Padding'
description: 'Padding is the space between each row. Every row has some padding at the top and bottom (default). "Remove padding" means removing the padding. "Reduce padding" removes the top or bottom padding plus the padding of the row above or below'
required: true
translatable: false
default_value:
  -
    value: default-padding
default_value_callback: ''
settings: {  }
field_type: list_string
