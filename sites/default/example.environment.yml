databases:
  default:
    default:
      database: rnd17
      username: root
      password: password
      prefix:
      host: 127.0.0.1
      port:
      namespace: Drupal\Core\Database\Driver\mysql
      driver: mysql

settings:
  rabbitmq_credentials:
    host: 127.0.0.1
    vhost: /
    port: 5672
    username: guest
    password: guest
  queue_service_esu: queue.rabbitmq
  hash_salt: kzWT4Q5kJe2DkfS72PrATBUfkw54RKzMCbQg933K1Qwe0ZKtonOV_xdmuCac
  #trusted_host_patterns: ['^rnd17-test\.sys\.comicrelief\.com$', '^127\.0\.0\.1$', '^localhost$']

config:
  system.performance:
    css:
      preprocess: false
    js:
      preprocess: false
  system.logging:
    error_level: verbose

config_dir: sites/default/files/config
